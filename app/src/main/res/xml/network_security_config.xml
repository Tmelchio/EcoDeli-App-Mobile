<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Configuration pour permettre le trafic HTTP vers votre serveur API -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Votre serveur API de production -->
        <domain includeSubdomains="false">88.172.140.59</domain>

        <!-- Pour le développement local -->
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>

        <!-- Pour les réseaux locaux (si nécessaire) -->
        <domain includeSubdomains="true">192.168.1.0</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
    </domain-config>

    <!-- Configuration par défaut : HTTPS uniquement pour les autres domaines -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
</network-security-config>